<Window x:Class="GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI"
               mc:Ignorable="d"
        Title="ADLE Simülasyonu" Height="600" Width="800" MinWidth="450">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="370"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Alanlar ve Nesneler" Grid.Column="0" Margin="3">
            <Grid Margin="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="150"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <GroupBox Header="Alan Adı" BorderThickness="0" Grid.Column="0" Grid.Row="0" >
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                    <TextBox VerticalContentAlignment="Center" Margin="3" x:Name="txtAreaName"   TextWrapping="Wrap" Grid.Column="0" />
                        <Button x:Name="button" Content=" + " Grid.Column="2" Grid.Row="0" Click="button_Click" Margin="3" />
                    </Grid>
                </GroupBox>
              

                <GroupBox Header="Nesne IP" BorderThickness="0" Grid.Column="2" Grid.Row="0" >
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <TextBox VerticalContentAlignment="Center"  x:Name="txtIP" TextWrapping="Wrap" Margin="3"/>
                        <Button x:Name="button_Copy" Content=" + "  Grid.Column="2" Grid.Row="1"  Margin="3" Click="button_NesneEkle_Click" />
                    </Grid>
                </GroupBox>

              

                <GroupBox Grid.Row="2"  Grid.Column="0"  Header="Ekli Nesneler" BorderThickness="0" >
                    <ListBox Margin="3" x:Name="listbox_Items" Width="155" SelectionChanged="listbox_Items_SelectionChanged"/>
                </GroupBox>
                <GroupBox Grid.Row="2"  Grid.Column="2"  Header="Aksiyonlar" BorderThickness="0" >
                    <ListBox Margin="3" x:Name="listbox_Items_actions" Width="155" Grid.Row="3" />
                </GroupBox>
                <GroupBox Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="4" Header="Ekli Alanlar" BorderThickness="0">
                    <TreeView Margin="3" x:Name="treeView" 
                  SelectedItemChanged="treeView_SelectedItemChanged" >
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Items}">
                                <TextBlock Text="{Binding AreaName}"></TextBlock>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>

                </GroupBox>
            </Grid>
        </GroupBox>

        <GroupBox Header="Dinleme" Grid.Column="2" BorderThickness="1" Margin="3">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="27"></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="75"></ColumnDefinition>
                    <ColumnDefinition Width="75"></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Button x:Name="btnBasla" Click="btnBasla_Click" Margin="3"  Content="Başla" Grid.Column="0" Grid.Row="0"   />

                <Button x:Name="btnDurdur" Click="btnDurdur_Click" Margin="3"  Content="Dur"  Grid.Column="1" Grid.Row="0" />

                <GroupBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Header="Toplanan Anılar" BorderThickness="0" Margin="3" >
                <ListBox x:Name="Memories"   Margin="3" ItemsSource="{Binding CaughtedMoments}" >
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Label Content="{Binding Path=BasiclyShow}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                </GroupBox>
            </Grid>
        </GroupBox>













    </Grid>
</Window>
